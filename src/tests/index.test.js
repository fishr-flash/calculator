describe('', () => {

    test( "check jest on work", ()=>{
        console.log( "hello world");
        const f = 2;
        const s = 3;
        const funct = ( f, s ) => f + s;
        expect( funct( f, s ) ).toBe( 4 );
    });
});

    test( "check jest on work", ()=>{
    console.log( "hello world");
    const f = 2;
    const s = 3;
    const funct = ( f, s ) => f + s;
    expect( funct( f, s ) ).toBe( 4 );
    });

